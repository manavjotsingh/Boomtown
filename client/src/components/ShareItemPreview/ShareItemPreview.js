import React from "react";
import ItemPreviewProvider, { ItemPreviewContext } from '../../context/ItemPreviewProvinder'

const ShareItemPreview =
    <ItemPreviewContext.Consumer>
        {({ state }) => (
            <div className=(classes.root) >
                   </div>
                )}


</ItemPreviewContext.Consumer >